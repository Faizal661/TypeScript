// TypeScript often infers types automatically, so you don't always need explicit annotations.
//  However, explicit annotations can be helpful for clarity and to guide the compiler.

let message = "Hello, world!"; // Inferred as string
let count = 10; // Inferred as number